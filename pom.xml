<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.radixiot</groupId>
    <artifactId>bacnet-sc-reference-stack</artifactId>
    <version>${revision}</version>
    <name>BACnet Secure Connect Reference Stack</name>
    <description>BACnet International reference implementation of BACnet Secure Connect (BACnet/SC). This fork adds the
        changes needed to drive the stack from Mango integration tests.
    </description>
    <url>https://github.com/RadixIoT/bacnet-sc-reference-stack</url>
    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://raw.githubusercontent.com/RadixIoT/bacnet-sc-reference-stack/main/LICENSE.txt</url>
        </license>
    </licenses>
    <organization>
        <name>Radix IoT, LLC</name>
        <url>https://radixiot.com/</url>
    </organization>
    <properties>
        <!-- Upstream BSCref version this fork is based on, plus a fork-level patch number. -->
        <revision>3.1.0-SNAPSHOT</revision>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <skipTests>false</skipTests>
    </properties>
    <build>
        <!-- Upstream layout is kept as-is so merges from the SourceForge original stay clean. -->
        <sourceDirectory>dev/src/java</sourceDirectory>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>3.1.3</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>3.4.2</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.6.0</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                    <outputDirectory>${project.build.directory}</outputDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>dev.bscs.common.Application</mainClass>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>1.7.28</version>
        </dependency>

        <!-- The stack configures logback directly (dev.bscs.common.Log), but consumers such as Mango bring their
             own SLF4J binding, so these must not be transitive. -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.2.3</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-core</artifactId>
            <version>1.5.34</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
    <issueManagement>
        <url>https://github.com/RadixIoT/bacnet-sc-reference-stack/issues</url>
        <system>github</system>
    </issueManagement>
    <scm>
        <url>https://github.com/RadixIoT/bacnet-sc-reference-stack</url>
        <connection>scm:git:https://github.com/RadixIoT/bacnet-sc-reference-stack.git</connection>
        <developerConnection>
            scm:git:[push=]https://github.com/RadixIoT/bacnet-sc-reference-stack.git[fetch=]https://github.com/RadixIoT/bacnet-sc-reference-stack.git
        </developerConnection>
        <tag>HEAD</tag>
    </scm>
    <distributionManagement>
        <repository>
            <id>github</id>
            <url>https://maven.pkg.github.com/RadixIoT/bacnet-sc-reference-stack</url>
        </repository>
        <snapshotRepository>
            <id>github</id>
            <url>https://maven.pkg.github.com/RadixIoT/bacnet-sc-reference-stack</url>
        </snapshotRepository>
    </distributionManagement>
</project>
